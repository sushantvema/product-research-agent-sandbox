# Neovim Config

This is the repo-owned Neovim configuration that gets synced into sandbox home state.

Keep editor-specific files here, even if the rest of `config/` is reorganized later.

This tree is intentionally tool-native and can stay deeper than the rest of the config layout.
