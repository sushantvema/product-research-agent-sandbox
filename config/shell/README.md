# Shell Snippets

This directory holds small shell features that are worth keeping separate from the main `bashrc`.

Use it for focused, reusable snippets like FZF integration.

Do not move broad shell initialization logic out of `config/bashrc` unless it becomes a clearly separate concern.
