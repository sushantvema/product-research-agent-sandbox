# Nix Layer

This directory defines the sandbox toolchain and shell composition.

Keep package selection and Nix-specific build wiring here.

Do not put general runtime shell scripts or workspace bootstrap logic here.
