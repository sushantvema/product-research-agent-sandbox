# To Do

- [x] Add the TypeScript compiler to the sandbox toolchain.
- [x] Rebuild the sandbox at a convenient pause so the newly added Go toolchain is available inside the container.
- [x] Port the reverse-i-search FZF setup from the global config into this sandbox control plane after the initial maintenance workflow settles.
- [x] Rebuild the sandbox at a convenient pause and verify `lazygit` and `fzf` inside the container.
- [x] Decide whether the reverse-i-search setup should live in `config/bashrc` directly or in a separate sourced shell snippet.
