# To Do

- [ ] Port the reverse-i-search FZF setup from the global config into this sandbox control plane after the initial maintenance workflow settles.
- [ ] Rebuild the sandbox at a convenient pause and verify `lazygit` and `fzf` inside the container.
- [ ] Decide whether the reverse-i-search setup should live in `config/bashrc` directly or in a separate sourced shell snippet.
